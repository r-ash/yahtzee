# yahtzee

This repo contains a yahtzee solver and API for interacting with the solved game.

## Usage

To run solver

```
cargo run --release --bin solve
```

To run the API

```
cargo run --release --bin yahtzee-api
```